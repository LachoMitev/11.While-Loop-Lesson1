#include <iostream>
#include <string>

int main()
{
	int sum = 0;
	
	std::string text;
	std::cin >> text;

	while (text != "Stop")
	{
		int n = stoi(text);
		sum += n;
		std::cin >> text;
	}

	std::cout << sum << std::endl;

	return 0;
}
